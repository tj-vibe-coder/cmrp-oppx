# Development Environment Configuration Template
# Copy this file to .env.development and fill in the actual values
# NEVER commit the actual .env.development file to git!

NODE_ENV=development

# Database Configuration (Local PostgreSQL)
# Replace with your actual development database URL
DATABASE_URL=postgresql://username:password@localhost:5432/your_dev_database?sslmode=disable

# JWT secret for development (can be simpler for dev)
JWT_SECRET=your-dev-jwt-secret-here

# Port for development server
PORT=3000

# Frontend URL for CORS (development)
FRONTEND_URL=http://localhost:8080

# API Base URL (development)
API_BASE_URL=http://localhost:3000

# App Configuration
APP_NAME=CMRP Opps Management (Dev)
VERSION=1.0.0-dev
